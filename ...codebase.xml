This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.gitignore
eslint.config.js
index.html
package.json
public/data/mockdata/data.ts
public/data/types/types.ts
public/logo.png
README.md
src/App.css
src/Components/Dashboard/Cards/Cards.tsx
src/Components/Dashboard/Dashboard.tsx
src/Components/Dashboard/Navbar/Navbar.tsx
src/Components/Insights/Insights.tsx
src/Components/Layouts/Dashboard/RootLayout.tsx
src/Components/Statcard/StatCard.tsx
src/Components/ToggleRole/ToggleRole.tsx
src/Components/Transactions/Transactions.tsx
src/main.tsx
src/store/useStore.ts
tsconfig.app.json
tsconfig.json
tsconfig.node.json
vite.config.ts
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="src/Components/Statcard/StatCard.tsx">
import { Wallet, TrendingUp, TrendingDown } from 'lucide-react';

interface StatCardProps {
    title: string;
    amount: number;
    type: 'balance' | 'income' | 'expense';
    percentage: number;
}

const StatCard = ({ title, amount, type, percentage }: StatCardProps) => {
    const formattedUSD = new Intl.NumberFormat('en-US', {
        style: 'currency',
        currency: 'USD',
    }).format(amount);

    const icons = {
        balance: <Wallet className="text-primary" size={20} />,
        income: <TrendingUp className="text-tertiary" size={20} />,
        expense: <TrendingDown className="text-red-500" size={20} />,
    };

    const bgColors = {
        balance: 'bg-primary/10',
        income: 'bg-tertiary/10',
        expense: 'bg-red-50',
    };

    return (
        <div className="bg-white p-8 rounded-4xl shadow-sm border border-secondary/5 flex flex-col gap-4 hover:shadow-md transition-all">
            <div className="flex justify-between items-start">
                <div className={`p-3 rounded-xl ${bgColors[type]}`}>
                    {icons[type]}
                </div>
                {percentage !== undefined && (
                    <span className={`px-2 py-1 rounded-lg text-[10px] font-bold ${percentage > 0 ? 'bg-tertiary/20 text-tertiary' : 'bg-red-100 text-red-600'}`}>
                        {percentage > 0 ? `+${percentage}%` : `${percentage}%`}
                    </span>
                )}
            </div>

            <div>
                <p className="text-neutral/40 inter text-xs font-bold uppercase tracking-widest">{title}</p>
                <h2 className="text-3xl font-black text-secondary manrope mt-1">{formattedUSD}</h2>
            </div>

            <div className="mt-2 h-1.5 w-full bg-background rounded-full overflow-hidden">
                <div
                    className={`h-full rounded-full ${type === 'balance' ? 'bg-primary' : type === 'income' ? 'bg-tertiary' : 'bg-red-400'}`}
                    style={{ width: '75%' }}
                />
            </div>
        </div>
    );
}

export default StatCard;
</file>

<file path="src/Components/ToggleRole/ToggleRole.tsx">
import { useStore } from "../../store/useStore";

export const ToggleRole = () => {
    const { role, setRole } = useStore();

    return (
        <div className="flex items-center gap-3 p-2 rounded-xl  ">
            <div className="flex bg-background p-3 rounded-lg gap-2">
                {(['User', 'Admin'] as const).map((r) => (
                    <button
                        key={r}
                        onClick={() => setRole(r)}
                        className={`px-3 py-1 text-xs font-bold transition-all rounded-md ${role === r
                            ? 'bg-primary text-white shadow-md'
                            : 'text-neutral/50 hover:bg-primary hover:text-white'}`}>
                        {r}
                    </button>
                ))}
            </div>
        </div>
    );
};
</file>

<file path=".gitignore">
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
</file>

<file path="eslint.config.js">
import js from '@eslint/js'
import globals from 'globals'
import reactHooks from 'eslint-plugin-react-hooks'
import reactRefresh from 'eslint-plugin-react-refresh'
import tseslint from 'typescript-eslint'
import { defineConfig, globalIgnores } from 'eslint/config'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      js.configs.recommended,
      tseslint.configs.recommended,
      reactHooks.configs.flat.recommended,
      reactRefresh.configs.vite,
    ],
    languageOptions: {
      ecmaVersion: 2020,
      globals: globals.browser,
    },
  },
])
</file>

<file path="public/data/mockdata/data.ts">
export const SUMMARY_STATS = {
    totalBalance: 45230.12, // [cite: 10]
    monthlyIncome: 8200.00, // [cite: 13]
    monthlyExpenses: 3450.50, // [cite: 23]
    netCashflow: 48229.40, // [cite: 95]
    savingsRate: 15, // [cite: 15, 156]
    projectedBalance: 48000, // [cite: 158]
};

export const BALANCE_TREND = [
    { date: '01 Oct', balance: 35000 }, // [cite: 31]
    { date: '08 Oct', balance: 38000 }, // [cite: 32]
    { date: '15 Oct', balance: 41000 }, // [cite: 33]
    { date: '22 Oct', balance: 44000 }, // [cite: 34]
    { date: '30 Oct', balance: 45230 }, // [cite: 35]
];

export const SPENDING_BREAKDOWN = [
    { category: 'Housing', value: 42, color: '#0047AB' }, // [cite: 27, 28]
    { category: 'Food & Beverage', value: 28, color: '#006B3C' }, // [cite: 29, 30]
    { category: 'Transport', value: 15, color: '#C41E3A' }, // [cite: 36, 37]
    { category: 'Entertainment', value: 15, color: '#666666' }, // [cite: 38, 39]
];

export const MONTHLY_COMPARISON = [
    { month: 'Jan', income: 7500, expenses: 3200 }, // [cite: 161, 162, 163]
    { month: 'Feb', income: 7800, expenses: 3100 }, // [cite: 164]
    { month: 'Mar', income: 8100, expenses: 3500 }, // [cite: 165]
    { month: 'Apr', income: 8200, expenses: 3450 }, // [cite: 166]
];

export type Role = 'Admin' | 'User';

export interface Transaction {
    id: string;
    date: string;
    amount: number;
    category: 'Food' | 'Housing' | 'Technology' | 'Income' | 'Transport' | 'Entertainment';
    type: 'Income' | 'Expense';
    description: string;
    status: 'Completed' | 'Pending';
}

export const INSIGHTS = [
    {
        id: 'i1',
        title: 'Dining Out Alert', // [cite: 181]
        description: 'Your spending on Dining Out increased by 20% this week.', // [cite: 181]
        type: 'warning',
    },
    {
        id: 'i2',
        title: 'Optimization Opportunity', // [cite: 256]
        description: 'Your subscription to "StreamCloud" hasn\'t been used in 30 days.', // [cite: 257]
        type: 'info',
    },
];
</file>

<file path="public/data/types/types.ts">
export type Role = 'Admin' | 'User';

export interface Transaction {
    id: string;
    date: string;
    amount: number;
    category: 'Food' | 'Rent' | 'Salary' | 'Tech' | 'Entertainment';
    type: 'Income' | 'Expense';
    description: string;
}
</file>

<file path="src/Components/Dashboard/Cards/Cards.tsx">
import StatCard from "../../Statcard/StatCard";
import { useStore, selectTotals } from "../../../store/useStore";
// FIX: Import shallow from zustand
import { shallow } from "zustand/shallow";

const Cards = () => {
    // FIX: Add 'shallow' as the second argument to useStore
    const { totalBalance, monthlyIncome, monthlyExpenses } = useStore(selectTotals, shallow);

    return (
        <div className="flex flex-col gap-6 pt-10">
            <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
                <StatCard title="Total Balance" amount={totalBalance} type="balance" percentage={75} />
                <StatCard title="Monthly Income" amount={monthlyIncome} type="income" percentage={5} />
                <StatCard title="Monthly Expenses" amount={monthlyExpenses} type="expense" percentage={-2} />
            </div>
            {/* ... rest of your UI */}
        </div>
    );
}

export default Cards;
</file>

<file path="src/Components/Dashboard/Navbar/Navbar.tsx">
import { ArrowDownToLine, BellDot, Plus, Search } from 'lucide-react'
import { ToggleRole } from '../../ToggleRole/ToggleRole'

const Navbar = () => {
    return (
        <nav >
            <div className="flex justify-between">
                {/* searchbar */}
                <div className="relative max-w-65 flex justify-center items-center">
                    <div className="absolute left-3 top-1/2 -translate-y-1/2  ">
                        <Search size={20} />
                    </div>
                    <input
                        type="text"
                        placeholder="Search ..."
                        className="text-xs md:text-sm w-full bg-[#f1f3ff] rounded-2xl pl-10 pr-4 py-3 text-[#434654] outline-none focus:ring-2 focus:ring-blue-400 transition-all placeholder:text-[#434654]"
                    />
                </div>

                <div className="flex items-center gap-5">
                    <ToggleRole></ToggleRole>
                    <BellDot size={21} className="text-[#434654] rounded-full hidden"></BellDot>
                    <div className="hidden lg:flex justify-center items-center gap-3 border-l border-black/30 scale-85">
                        <div className="flex-col text-right">
                            <p className=" font-medium text-base text-black/90">John Doe</p>
                            <p className=" pl-4 text-sm text-black/70">Chief Financial Officer</p>
                        </div>
                        <img src="https://pbs.twimg.com/profile_images/1862717563311968256/xfgt1L9l_400x400.jpg" alt="user-image"
                            className="size-12 rounded-full" />
                    </div>
                </div>
            </div>

            <div className="hidden lg:flex justify-between items-center pt-5">
                <div>
                    <h1 className="hidden lg:block text-2xl text-black font-bold pb-1">Financial Overview</h1>
                    <h3 className="hidden lg:block text-black/70 font-medium">Aggregated data for fiscal year 2026</h3>
                </div>

                <div className="gap-2 hidden lg:flex">
                    <button className="flex justify-center items-center gap-1 px-3 py-3 rounded-xl text-sm text-primary font-bold bg-[#E2E8FC] hover:bg-[#E2E8FC]/75 transition-all duration-100 ease-in-out"><ArrowDownToLine size={18}></ArrowDownToLine>
                        <p className="">Export Report</p>
                    </button>

                    <button className="flex justify-center items-center gap-1 px-3 py-3 rounded-xl text-sm text-white font-bold bg-primary hover:bg-primary/75 transition-all duration-100 ease-in-out"><Plus size={18}></Plus>
                        <p className="">Add Transaction</p>
                    </button>
                </div>
            </div>
        </nav>
    )
}

export default Navbar
</file>

<file path="src/store/useStore.ts">
import { create } from 'zustand';
import { persist } from 'zustand/middleware';
import type { Role } from '../../public/data/types/types';
import { SUMMARY_STATS, type Transaction } from '../../public/data/mockdata/data';

interface FinanceState {
    transactions: Transaction[];
    role: Role;
    setRole: (role: Role) => void;
    addTransaction: (tx: Transaction) => void;
}

export const useStore = create<FinanceState>()(
    persist(
        (set) => ({
            transactions: [],
            role: 'User',
            setRole: (role) => set({ role }),
            addTransaction: (tx) =>
                set((state) => ({
                    transactions: [tx, ...state.transactions]
                })),
        }),
        { name: 'finance-storage' }
    )
);

export const selectTotals = (state: FinanceState) => {
    if (state.transactions.length === 0) {
        return {
            totalBalance: SUMMARY_STATS.totalBalance,
            monthlyIncome: SUMMARY_STATS.monthlyIncome,
            monthlyExpenses: SUMMARY_STATS.monthlyExpenses,
        };
    }

    const income = state.transactions
        .filter((t) => t.type === 'Income')
        .reduce((acc, t) => acc + t.amount, 0);

    const expenses = state.transactions
        .filter((t) => t.type === 'Expense')
        .reduce((acc, t) => acc + Math.abs(t.amount), 0);

    return {
        totalBalance: income - expenses,
        monthlyIncome: income,
        monthlyExpenses: expenses,
    };
};
</file>

<file path="tsconfig.app.json">
{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "target": "ES2023",
    "useDefineForClassFields": true,
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "types": ["vite/client"],
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true
  },
  "include": ["src"]
}
</file>

<file path="tsconfig.json">
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
</file>

<file path="tsconfig.node.json">
{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
    "target": "ES2023",
    "lib": ["ES2023"],
    "module": "ESNext",
    "types": ["node"],
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true
  },
  "include": ["vite.config.ts"]
}
</file>

<file path="index.html">
<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <link rel="icon" type="image/svg+xml" href="/logo.png" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Zorvyn Finance Dashboard</title>
</head>
<body>
  <div id="root"></div>
  <script type="module" src="/src/main.tsx"></script>
</body>
</html>
</file>

<file path="README.md">

</file>

<file path="src/Components/Insights/Insights.tsx">
const Insights = () => {
    return (
        <section className="h-screen bg-[#faf8ff] rounded shadow-xl p-5"></section>
    )
}

export default Insights
</file>

<file path="src/Components/Transactions/Transactions.tsx">
const Transactions = () => {
    return (
        <section className="h-screen bg-[#faf8ff] rounded shadow-xl p-5"></section>
    )
}

export default Transactions
</file>

<file path="vite.config.ts">
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'


// https://vite.dev/config/
export default defineConfig({
  plugins: [react(), tailwindcss()],
})
</file>

<file path="package.json">
{
  "name": "zorvyn-finance",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.2.2",
    "@tailwindcss/vite": "^4.2.2",
    "clsx": "^2.1.1",
    "date-fns": "^4.1.0",
    "framer-motion": "^12.38.0",
    "lucide-react": "^1.3.0",
    "postcss": "^8.5.8",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "react-router-dom": "^7.13.2",
    "recharts": "^3.8.1",
    "tailwindcss": "^4.2.2",
    "zustand": "^5.0.12"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/node": "^24.12.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "eslint": "^9.39.4",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.4.0",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.57.0",
    "vite": "^8.0.1"
  }
}
</file>

<file path="src/main.tsx">
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import { createBrowserRouter, RouterProvider } from 'react-router-dom';
import './App.css'
import Dashboard from './Components/Dashboard/Dashboard.tsx';
import Rootlayout from './Components/Layouts/Dashboard/RootLayout.tsx';
import Transactions from './Components/Transactions/Transactions.tsx';
import Insights from './Components/Insights/Insights.tsx';

const router = createBrowserRouter([
  {
    path: "/",
    element: <Rootlayout />,
    children:
      [{ index: true, element: <Dashboard />, },
      { path: "transactions", element: <Transactions />, },
      { path: "insights", element: <Insights />, },
      ],
  },
])

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <RouterProvider router={router} />
  </StrictMode>,
)
</file>

<file path="src/Components/Dashboard/Dashboard.tsx">
import Cards from "./Cards/Cards"
import Navbar from "./Navbar/Navbar"

const Dashboard = () => {
    return (
        <section className="min-h-screen bg-[#faf8ff] rounded shadow-xl px-4 lg:px-10 py-3">
            <Navbar></Navbar>
            <Cards></Cards>
        </section>

    )
}

export default Dashboard
</file>

<file path="src/App.css">
@import url('https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&family=Manrope:wght@200..800&display=swap');
@import "tailwindcss";

@theme {
    --color-primary: oklch(0.4802 0.2012 260.48);
    --color-secondary: oklch(0.3726 0.0328 258.99);
    --color-tertiary: oklch(0.6959 0.1491 162.48);
    --color-neutral: oklch(0.2103 0.0294 266.12);
    --color-background: oklch(0.9696 0.0147 281.96);
}

.manrope {
    font-family: "Manrope", sans-serif;
    font-optical-sizing: auto;
    font-weight: 500;
    font-style: normal;
}

.inter {
    font-family: "Inter", sans-serif;
    font-optical-sizing: auto;
    font-weight: 500;
    font-style: normal;
}

* {
    font-family: "Manrope", sans-serif;
}

body {
    background-color: oklch(0.9696 0.0147 281.96);
}

.aside-button {
    font-family: "Inter", sans-serif;
    font-weight: 400;
}
</file>

<file path="src/Components/Layouts/Dashboard/RootLayout.tsx">
import { Outlet, NavLink } from 'react-router-dom';
import { LayoutDashboard, MessageCircleQuestionMark, NotebookTabs, UserRoundCog, WandSparkles } from 'lucide-react';

export default function RootLayout() {
    return (
        <div className="flex flex-col lg:flex-row h-screen overflow-hidden">
            <aside className="lg:w-72 flex  bg-[#f1f3ff] text-white lg:p-4 shadow-2xl justify-center lg:justify-start
            fixed bottom-0 left-0 right-0 lg:relative lg:flex-col">
                <a href="https://zorvyn.io/" target='_blank' className="hidden lg:block">
                    <img src="logo.png" alt="navbar-logo" className="h-15 mx-auto px-6 my-3" />
                    <p className="text-primary leading-7 max-w-35 text-center mx-auto text-2xl font-bold">Zorvyn Finance</p>
                </a>

                <nav className="flex flex-row py-4 lg:flex-col gap-3 text-[#5c5e6a] lg:pt-12 ">
                    <NavLink to="/" className={({ isActive }) =>
                        `flex flex-col lg:flex-row items-center gap-1.5 lg:gap-3 text-xs lg:text-base
                             px-5 lg:px-6 py-2 lg:py-3 rounded-2xl lg:rounded transition-all duration-300 ease-in-out
                             ${isActive
                            ? 'bg-primary/30 shadow-lg text-[#003d9b]'
                            : 'hover:bg-primary/30 hover:shadow-lg hover:text-[#003d9b]'
                        }`
                    }
                    >
                        <LayoutDashboard size={22} strokeWidth={2} />
                        <p className="lg:block lg:text-16px inter aside-button ">Overview</p>
                    </NavLink>

                    <NavLink to="/transactions"
                        className={({ isActive }) =>
                            `flex flex-col lg:flex-row items-center gap-1.5 lg:gap-3 text-xs lg:text-base
                             px-5 lg:px-6 py-2 lg:py-3 rounded-2xl transition-all duration-300 ease-in-out
                             ${isActive
                                ? 'bg-primary/30 shadow-lg text-[#003d9b]'
                                : 'hover:bg-primary/30 hover:shadow-lg hover:text-[#003d9b]'
                            }`
                        }
                    >
                        <NotebookTabs size={22} strokeWidth={2} />
                        <p className=" lg:block inter aside-button ">Transactions</p>
                    </NavLink>

                    <NavLink to="/insights" className={({ isActive }) =>
                        `flex flex-col lg:flex-row items-center gap-1.5 lg:gap-3 text-xs lg:text-base
                             px-5 lg:px-6 py-2 lg:py-3 rounded-2xl transition-all duration-300 ease-in-out
                             ${isActive
                            ? 'bg-primary/30 shadow-lg text-[#003d9b]'
                            : 'hover:bg-primary/30 hover:shadow-lg hover:text-[#003d9b]'
                        }`
                    }
                    >
                        <WandSparkles size={22} strokeWidth={2} />
                        <p className=" lg:block inter aside-button ">Insights</p>
                    </NavLink>
                </nav>


                <nav className="hidden lg:flex flex-col text-[#5c5e6a] border-t border-t-neutral-400/50 mt-auto">
                    <NavLink to="/settings" className="flex items-center gap-1 px-6 py-3 rounded-2xl transition-all duration-300 ease-in-out">
                        <UserRoundCog size={22} strokeWidth={2} />
                        <p className=" lg:block inter aside-button ">Settings</p>
                    </NavLink>
                    <NavLink to="/support" className="flex items-center gap-1 px-6 py-3 rounded-2xl transition-all duration-300 ease-in-out">
                        <MessageCircleQuestionMark size={22} strokeWidth={2} />
                        <p className=" lg:block inter aside-button ">Support</p>
                    </NavLink>
                </nav>
            </aside>

            <main className="flex-1 px-5 overflow-y-auto">
                <Outlet />
            </main>
        </div>
    );
}
</file>

</files>
